---
- hosts: exemple-1
  remote_user: root
  tasks:
    - name: Installation de nginx web server
      package:
        name: nginx
        state: installed
      notify:
        - start nginx
        
  handlers:
    - name: start nginx
      service:
        name: nginx
        state: started
